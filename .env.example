PORT=4000
MISTRA_API_URL=https://api.mistra.example/v1/generate
MISTRA_API_KEY=3aUoIQOYV7fg7VR0ZeS0y34u2eLFsNr6
MISTRA_CHAT_MAX_WORDS=45

# MongoDB connection (required for Google auth persistence)
MONGO_URI=mongodb://localhost:27017/careerpath
MONGO_DB=careerpath

# JWT secret for signing auth tokens
JWT_SECRET=change_this_dev_secret

# Auth mode: currently Google Sign-In only; local registration & password login disabled.

# (Future) Path to Firebase service account if verifying ID tokens server-side
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Enable verbose debug logging for AI calls
# MISTRA_DEBUG=1
